#pragma once

#include <juce_graphics/juce_graphics.h>

namespace coolsynth::ui::palette
{
    inline const auto panelBlack = juce::Colour::fromRGB(6, 9, 10);
    inline const auto panelRaised = juce::Colour::fromRGB(18, 23, 25);
    inline const auto panelRaisedAlt = juce::Colour::fromRGB(28, 34, 37);
    inline const auto panelStroke = juce::Colour::fromRGB(63, 71, 75);
    inline const auto textPrimary = juce::Colour::fromRGB(244, 244, 239);
    inline const auto textSecondary = juce::Colour::fromRGB(166, 173, 176);
    inline const auto ledGreen = juce::Colour::fromRGB(123, 245, 96);
    inline const auto ledGreenDim = juce::Colour::fromRGB(45, 77, 40);
    inline const auto ledTextOff = juce::Colour::fromRGB(139, 255, 121);
    inline const auto ledTextOn = juce::Colour::fromRGB(12, 18, 12);
    inline const auto learnYellow = juce::Colour::fromRGB(255, 214, 76);
    inline const auto badgeGreen = juce::Colour::fromRGB(124, 210, 116);
    inline const auto tooltipBackground = juce::Colour::fromRGB(38, 45, 48);
    inline const auto tooltipBorder = juce::Colour::fromRGB(255, 171, 82);
}
